# Using Search boxes on the Portal
Most Search boxes on the Portal are standardised, so this advice should apply everywhere.

The complete search string will be split into individual words. For example, if you search for "red and blue", results with both "red" and "blue" (and "and") will be returned.
